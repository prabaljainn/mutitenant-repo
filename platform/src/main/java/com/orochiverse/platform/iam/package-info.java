/**
 * Operator-side IAM module: tenants, operator users, operator-to-tenant
 * assignments, and {@code /admin/api/**} endpoints. Depends on {@code common}.
 * Must NOT depend on {@code tenant} or {@code gcs}.
 */
package com.orochiverse.platform.iam;
