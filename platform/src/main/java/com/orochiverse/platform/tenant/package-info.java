/**
 * Tenant-admin self-service module: managing the current tenant's own users
 * and {@code /api/tenant/**} endpoints. Depends on {@code common}.
 * Must NOT depend on {@code iam} or {@code gcs}.
 */
package com.orochiverse.platform.tenant;
