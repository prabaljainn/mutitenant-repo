Hi [(${firstName})],

A password reset was requested for your Orochiverse account
([(${email})]).

Set a new password using this link:

  [(${resetUrl})]

This link expires on [(${expiresAt})] and can only be used once.

If you didn't request this, you can ignore this email. Your current
password remains valid.

-- Orochiverse Platform
